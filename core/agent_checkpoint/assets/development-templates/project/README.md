# New Project Template

Use this template when starting a new software project.

The planning flow is:

```text
Requirements
-> Architecture/Security decisions
-> dependency-ordered Phases
-> detailed Steps
-> one Gate per Phase
-> implementation one Step/Gate per invocation
```

The planning output should create a project-specific repository planning package.

Use `prompts/PLAN_NEW_PROJECT_PROMPT.md` first.
