Execute the planned release / deployment work.

First read:

- repository agent instructions;
- this work package's `WORK.md` or specific work-description file;
- `CURRENT.md`;
- current Step/Gate prompt;
- affected project docs/tests.

Execute ONLY the Current Target.

Follow all Release / Deployment-specific hard rules from this template and the generated work package.

Rules:

- one invocation = one Step or one Gate;
- record concrete evidence;
- preserve unrelated behavior;
- update `CURRENT.md` only after verified PASS;
- after completion report, STOP;
- do not begin the next target automatically.
